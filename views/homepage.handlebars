
{{>header}}

{{>title}}

  {{#each hotels as |hotel|}}
    <div class="">
        <div class="">
            <p>{{hotel.name}}</p> 
            <p>{{hotel.address}}</p> 
            <img src="/image/{{hotel.image}}.jpeg" alt="a" >
        </div>
        <div class="">
            {{hotel.description}}
        </div>
    </div>
{{/each}}
